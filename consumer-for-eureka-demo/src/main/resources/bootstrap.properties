server.port=8223
spring.application.name=consumer-eureka-demo

eureka.client.service-url.defaultZone=http://localhost:8221/eureka/
eureka.instance.metadata-map.instanceId=${spring.application.name}:${server.port}